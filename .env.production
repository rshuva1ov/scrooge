# Публичные клиентские значения для `vite build` (режим production).
# Vercel подхватывает этот файл при сборке. Дашборд может переопределить ключи.

VITE_SUPPORT_EMAIL=shuvalov.rem@mail.ru
VITE_DONATE_URL=https://dalink.to/harekuintv
